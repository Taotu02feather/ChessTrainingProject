我要构建一个完整的国际象棋 AI 系统，名为 “ChessMate”，包含三大核心功能：
1. 自行训练强化学习模型（基于 AlphaZero 或类似算法）。
2. 提供本地图形界面（GUI）进行人机对弈。
3. 能够通过屏幕截图识别网页上的棋局，并自动走子（网页对战）。

**我的要求：**
- 你拥有完全的自主权来设计项目结构、选择技术栈（Python 库）、定义算法细节。
- 但最终产出的代码必须：
  - 在 Windows 11 环境下运行，且能自动检测并使用我当前的 conda 环境（环境名：`chess_ai`）。
  - 包含详细的注释（每个类、函数、关键步骤均有中文或英文注释）。
  - 提供完整的项目文档（README.md 或 DOCUMENTATION.md），说明如何安装依赖、配置参数、启动各模式。
- 整个系统应易于扩展和维护，代码风格清晰（如 PEP8）。

**建议的交付物清单**（你可以在具体实现中增删）：
- `requirements.txt`（或 `environment.yaml`）：列出所有依赖，并注明哪些是必需的。
- 一个自动环境检查脚本 `check_env.py`，运行后报告当前环境已安装的包，并提示缺失的包。
- 训练模块：包含神经网络定义、MCTS、自对弈生成、经验回放、训练循环。
- 视觉识别模块：能够从屏幕截图检测棋盘并识别棋子，输出 FEN 字符串。
- 交互模块：实现模拟鼠标点击/拖拽，用于网页走子。
- GUI 模块：提供一个本地对弈窗口（建议使用 PyQt5 或 Tkinter）。
- 主入口脚本 `main.py`，提供命令行或菜单选择三种模式（训练/本地对弈/网页对战）。
- 配置文件 `config.yaml` 或 `config.py`，集中管理所有可调参数。
- 一个详细的 README，包含安装步骤、参数说明、运行示例、常见问题。

**特别说明：**
- 训练部分优先考虑可快速验证的小规模实验（例如先用 10 局自对弈测试流程），然后再扩展。
- 视觉识别可从简单场景开始（例如纯色背景的 chess.com 截图），并给出可选的改进方向。
- 网页交互建议使用 `pyautogui` + `PIL` 截图，通过坐标点击实现走子（先实现鼠标点击移动，未来可升级为拖拽）。
- 本地 GUI 需支持玩家选择执白或执黑，并显示走子历史和状态信息。
- 整个项目应包含错误处理和日志记录，方便调试。

请按以下步骤推进：
1. 首先，生成环境检测脚本，并告诉我需要安装哪些额外的库。
2. 然后，设计完整的项目目录结构，并给出每个文件的职责说明。
3. 依次实现每个模块，每次生成一个完整的文件，并附上说明。
4. 最后，汇总生成完整的 README 和使用示例，并且要写具体的更新日志。

**注意**：你不需要一次性完成所有内容，我会逐步反馈，你可以调整。请确保代码可直接运行，避免外部依赖的硬编码路径（使用相对路径或配置变量）。